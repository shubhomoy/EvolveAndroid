<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.evolve.evolve.EvolveActivities.SchoolFilterActivity">

    <include
        android:id="@+id/toolbar"
        layout="@layout/action_bar" />


    <EditText
        android:id="@+id/school_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:layout_margin="16dp"
        android:text="Name of the school"
        android:textSize="18sp" />

    <Spinner
        android:id="@+id/affilation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/school_name"
        android:layout_margin="16dp"
        android:prompt="@string/spinnerAffTitle" />

    <Spinner
        android:id="@+id/type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/affilation"
        android:layout_margin="16dp"
        android:prompt="@string/spinnerType" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_gravity="bottom|right"
        android:layout_margin="16dp"
        android:src="@mipmap/ic_done_white_24dp"
        app:layout_anchorGravity="bottom|right|end" />

</RelativeLayout>
